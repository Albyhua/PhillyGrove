{{#each this}}
  <section class="comment">
    <div class="meta">
      {{user.name}}
      {{formatDate created_at}}
    </div>
    <div class="text">
      {{comment_text}}
    </div>
  </section>
{{/each}}