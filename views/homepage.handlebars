<h2>Events:</h2>
{{#each events as |event|}}
  <div>
    <div>
      <h2>
        <a class="event" href="/event/{{event.id}}">{{event.title}}</a>
      </h2>
      <p class="userName">
        Created by
        {{event.user.name}}
      </p>
    </div>
  </div>
{{/each}}